# core/market/hyperliquid/（占位）

本目录预留给 **Hyperliquid 行情事实采集**（Market）相关代码与配置。

说明：
- 目标：把 Hyperliquid 的行情事实（例如 K 线、成交、资金费率、OI、基差等）采集并落库到事实层（TimescaleDB 或后续统一存储）。
- 边界：仅做“事实采集→落库”，不做派生指标计算、不做对外投递。
- 当前状态：空目录占位，保证目录结构在版本控制中稳定存在。

